#READ FOR CONVERSION FROM MP3 TO WAV FORMAT

#RUN run function after changing the paths 
#audios will have path where mp3 files are stored
#destination will have path where converted wav files will 
#be stored

#RUN THE run() function

#it will take 60 min for converting all testing and training mp3 files